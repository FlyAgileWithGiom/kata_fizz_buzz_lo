import { fizz, buzz, generateAll, fizzToStr } from "../src/fizz_buzz";


describe('hey we\'re working!', () => {
  test('the second test is touched', () => {
    expect(true).toBeTruthy();
  });

});
